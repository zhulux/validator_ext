require 'spec_helper'

describe ValidatorExt do
  it 'has a version number' do
    expect(ValidatorExt::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
